#include "a_Autonomous.h"

// Constructors ////////////////////////////////////////////////////////////////

Autonomous::Autonomous(Robot *p)
{
	robot = p;
}

void Autonomous::Task()
{
	/*if(ControllerSpeed == 1)
		robot->IntakeSpeed = 400;
	else if(ControllerSpeed == -1)
		robot->IntakeSpeed = -400;
	else
		robot->IntakeSpeed = 0;*/
	
}